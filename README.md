# SmartAPI_source
SmartAPI is an API gateway compatible with the OpenAI and Anthropic Messages protocols. Keep your existing SDKs and agents—just change the `base_url` to call the same models at lower cost.  This repo documents hands-on evaluations, stability observations, and integration notes for major LLM APIs.
